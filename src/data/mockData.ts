import { Brand, Product, Client, Invoice, Recovery, PaymentAccount } from "@/types";

// All mock data has been removed - the app uses Supabase for data persistence

export const initialBrands: Brand[] = [];

export const initialProducts: Product[] = [];

export const initialClients: Client[] = [];

export const initialInvoices: Invoice[] = [];

export const initialRecoveries: Recovery[] = [];

export const paymentAccounts: PaymentAccount[] = [];
